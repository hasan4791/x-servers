SHELL=/bin/bash
MAILTO=root@example.com
PATH=/bin:/sbin:/usr/bin:/usr/sbin:/usr/local/bin:/usr/local/sbin

SLACK_URL="{{ xserver_slack_url }}"
XSERVER_PATH="{{ xserver_root }}/x-servers"
XSERVER_DATA_PATH="{{ xserver_data }}/x-servers-data"
XSERVER_ARCH="{{ xserver_target_arch }}"

#Update node & images weekly
0 0 * * 0 root "${XSERVER_PATH}"/cronjobs/update-x-servers.sh
